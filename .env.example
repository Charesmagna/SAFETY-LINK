# Example environment for local development
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/safety_link_dev
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=safety_link_dev
JWT_SECRET=change-me
SMS_PROVIDER=mock
PORT=3000
# Optional: set a default tenant for local debugging (UUID)
APP_CURRENT_TENANT=
